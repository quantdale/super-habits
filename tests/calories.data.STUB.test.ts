/**
 * TODO: add data-layer tests (requires DB mocking setup)
 * Filename STUB marker — not real SQLite coverage yet.
 */
import { describe } from "vitest";

describe.skip("calories.data (SQLite)", () => {});
