async function globalTeardown() {
  // Reserved for future cleanup (e.g. stop test server, clear shared state)
  // Currently a no-op — Metro dev server is managed externally
}

export default globalTeardown;
