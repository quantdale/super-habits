Fresh session, zero chat memory.

Fetch/prune and reconcile `quantdale/super-habits` to the latest `origin/main` without discarding legitimate work.

Read `AGENTS.md`, `.agent/PLANS.md`, this change's `README.md`, every authoritative file it references, and `.agent/execplans/account-recovery-dist-sync-determinism.md`.

Execute OpenSpec change `fix-account-recovery-dist-sync-determinism` autonomously from start to finish. Reproduce and permanently fix the current dist-sync account/recovery CI failures. The repository-persisted proposal, design, normative spec, tasks, and ExecPlan are the source of truth. Keep them current. Do not fix by timeout inflation, retries, skipping, quarantine, or weakening production fail-closed account safety. Reconcile the Weekly Review ExecPlan's inaccurate exact-final-SHA green claim.

English only.

Finish only when all required QA passes, `npm run e2e:sync` is fully green, all completed work is committed/pushed to `main`, working tree is clean, only remote `main` remains, local `main == origin/main`, and the exact final SHA has GitHub Actions `quality` PASS and `e2e` PASS. Do not stop with CI pending or red.
