## 1. Baseline and contract hygiene

- [x] 1.1 Re-run the previously unverified persona/journey and deterministic simulation lanes, record seeds and known quarantines, and rerun sync E2E after the Windows build-script fix.
- [x] 1.2 Register and quarantine the pre-existing recurring-todo D14 performance contract gap without changing its threshold or product behavior.

## 2. Native workspace and accessibility

- [x] 2.1 Add the `.maestro/` workspace and focused semantic smoke, persistence, lifecycle, Pomodoro, and notification-path flows with tags and no arbitrary sleeps.
- [x] 2.2 Add only user-beneficial accessibility labels/roles required to address native controls semantically.
- [x] 2.3 Add a native preflight/runner script that reports missing tools, devices, builds, artifacts, replay commands, and six-value triage metadata.

## 3. Build and QA integration

- [x] 3.1 Add a stable iOS bundle identifier and dedicated EAS native E2E build profile without production secrets.
- [x] 3.2 Add a cost-conscious EAS Maestro workflow for explicit/manual Android and iOS execution and validate its local syntax.
- [x] 3.3 Add native npm commands, native gate entries, and feature-to-native mappings to the machine-readable impact map.
- [x] 3.4 Update AGENTS.md and testing/known-gap/autonomous-QA documentation with native prerequisites, failure handling, platform limitations, and change-specific escalation commands.

## 4. Validation and handoff

- [x] 4.1 Run typecheck, lint, unit/integration tests, web export, sync E2E, impact/OpenSpec validation, and focused/full available web lanes after implementation.
- [x] 4.2 Run native preflight commands on this host and classify unavailable Android/iOS infrastructure explicitly; run actual Maestro flows wherever a target exists.
- [x] 4.3 Update the final validation report with exact commands, pass/quarantine/blocker/not-run status, and the next native device/cloud phase.
