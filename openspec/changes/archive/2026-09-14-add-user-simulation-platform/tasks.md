## 0. Decisions — resolved (design.md D1–D10)

Settled before implementation; reasoning lives in design.md.

- [x] 0.1 **Relationship to the parent change** → follow-up platform layer over `add-real-world-user-simulation-testing`; its suite, gating role, and CG-1/CG-2 quarantines are untouched (spec § Reuse).
- [x] 0.2 **Execution model** → D4/D7: deterministic core gates; seeded variability is nightly/local; AI exploratory lane is opt-in and never gates.
- [x] 0.3 **Backend validation** → D8: disposable Supabase project on `main`/nightly with a hard production-isolation guard; PR lanes keep network-boundary fakes.
- [x] 0.4 **Model format** → D2: typed TypeScript builders (`definePersona`/`defineScenario`/`defineWorkflow`), validated at load; JSON only for artifacts.
- [x] 0.5 **Placement** → D1: top-level `simulation/`, importing `e2e/helpers/*`; no new runtime dependencies, no LLM SDK in the repo.

## 1. Model layer (`simulation/`)

Depends on the parent change's harness tasks 3.1–3.6 (`e2e/helpers/{journey,reset,clock,failure,seed,oracles}.ts`) being landed; if they are not, land them first or carry them as a blocking dependency note.

- [x] 1.1 Create the `simulation/` skeleton (D1 layout) with a root `README.md` documenting: what the platform is, the lane taxonomy (D10), and the decision rule for "deterministic journey (parent) vs. scenario (platform) vs. AI mission". **(`simulation/README.md`; finalized further in 10.2 with the matrix-backed lane table, authoring guide, repro workflow, isolation rules, and baselines/budgets.)**
- [x] 1.2 `simulation/model/types.ts`: TypeScript types for `Persona`, `Scenario`, `Workflow`, `SemanticStep`, `Oracle`, `BehaviorParams`, `RunMode` (`deterministic | seeded | exploratory`). **(Also `SectionName`/`SurfaceName`/`FixtureSize`/`InjectionKind` etc.; oracles discriminated on `kind`; validated by 1.4.)**
- [x] 1.3 `simulation/model/builders.ts`: `definePersona()`, `defineScenario()`, `defineWorkflow()` builders with sensible defaults (D2). **(Plus `defineModel()`, `defaultBehaviorParams()`, consts `DEFAULT_THINK_TIME`/`DEFAULT_SESSION_LENGTH`.)**
- [x] 1.4 `simulation/model/validate.ts`: `validateSimulationModel()` — pure function checking required fields, unknown feature/section names, out-of-range behavior params, oracle-less mutating steps (spec: Every step carries its own oracles), and dangling workflow/persona references. **(All five rule classes implemented; `isSimulationModelValid` convenience; wired into `sim:validate`.)**
- [x] 1.5 Unit tests (`tests/simulation.model.test.ts`): builder defaults, each validation rule firing, and a known-good model passing. **(33 tests.)**
- [x] 1.6 Semantic step catalog (`simulation/model/steps.ts`): the initial step set — navigation (`switchSection`, `openSettings`, `openCommand`), entity actions (`createTodo`, `toggleTodo`, `createHabit`, `tickHabit`, `logCalories`, `buildRoutine`, `startPomodoro`), realism (`waitThinkTime`, `maybeMakeMistake`, `abandonForm`), environment (`goOffline`, `goOnline`, `advanceClockToNextDay`, `injectFailure`, `reloadApp`), verification (`expectOracle`, `expectAcrossSurfaces`), and `apiLeg`. Each step maps to parent helpers — no selectors in scenario files. **(Catalog with `parentHelper` mapping + predicates; runner-owned steps noted as seams.)**

## 2. Behavior engine

- [x] 2.1 `simulation/behavior/rng.ts`: single seeded RNG (e.g., mulberry32 — no dependency) threaded through all sampling. **(`createSeededRng(seed)` with `next`/`range`/`int`/`chance`/`pick`/`normal`/`logNormal`.)**
- [x] 2.2 `simulation/behavior/thinkTime.ts`: per-persona clamped log-normal think-time sampler; `deterministic` mode returns fixed per-step values. **(`createThinkTimeSampler(params)` with `sample(rng)`/`fixed()`; `DEFAULT_THINK_TIME_MS` ≈ 665ms.)**
- [x] 2.3 `simulation/behavior/injectors.ts`: the bounded injector set — double-tap, typo+correction, mid-form abandonment, offline toggle, tab-hide during running timer — each implemented through user-reachable actions only, each logging its injection to the step log. **(`createInjectors()` — the five kinds; each `{kind, appliesTo, rate, decide}` pure; logs `StepLogEntry`.)**
- [x] 2.4 `simulation/behavior/engine.ts`: mode handling — `deterministic` (injectors off, fixed seed), `seeded` (seed chosen, recorded in run report), `exploratory` (AI lane; engine inactive). **(`buildRunPlan(steps, behavior, options)` → `RunPlan`; deterministic = zero injections + fixed think times; exploratory throws; seeded samples per persona rates, at most one injection per step.)**
- [x] 2.5 Unit tests (`tests/simulation.behavior.test.ts`): same seed → identical action sequence; injectors respect persona rate; `deterministic` mode emits zero injections; think times stay within clamps. **(21 tests.)**

## 3. Runner and API legs

- [x] 3.1 `simulation/runner/execute.ts`: executes a scenario step graph through Playwright against the served `dist/` export, reusing parent helpers for reset/seed/clock/failure/oracles; `workers: 1` semantics (single context per origin).
- [x] 3.2 API legs: `apiLeg` steps call real `*.data.ts` functions via `page.evaluate` (parent seeding approach) or the Supabase client (disposable lane only); guard rejects raw SQL strings in API legs.
- [x] 3.3 `simulation/runner/cli.ts` + `package.json` scripts: `sim:run` (scenario library, `--mode`, `--seed`, `--scenario` filters), `sim:validate` (model validation only).
- [x] 3.4 Playwright wiring: a dedicated `simulation` project in `playwright.config.ts` (or a standalone launcher if project semantics fight the runner) that leaves the existing `chromium` project untouched.
- [x] 3.5 Runner self-test: a smoke scenario exercising every step type end-to-end against the local build, run in `deterministic` mode twice asserting identical action logs (spec: Deterministic mode is reproducible).

## 4. Observability

- [x] 4.1 `simulation/observe/report.ts`: the `run-report.json` schema (run id, lane, environment, seed, scenario/persona refs, per-step entries with durations + evaluated oracles, artifact pointers, outcome) and a validator; unit-tested (`tests/simulation.report.test.ts`).
- [x] 4.2 `simulation/observe/artifacts.ts`: per-oracle screenshots, video/trace retention on failure, output layout under a gitignored `simulation-output/` directory.
- [x] 4.3 `simulation/observe/digest.ts`: failure-digest Markdown generator (persona, scenario, step, seed, expected vs. actual, state summary, artifact links) formatted for pasting into a defect issue.
- [x] 4.4 Verify one schema across lanes: scenario run, seeded run, and repro replay all emit reports passing the 4.1 validator.

## 5. Bug-reproduction packages

- [x] 5.1 `simulation/repro/bundle.ts`: bundle format writer — `bundle.json` metadata (commit, scenario ref, seed, TZ, browser, timestamps), SQLite export, AsyncStorage dump, `actions.jsonl` semantic action log, `trace.zip`, console/network logs, `narrative.md` template. **(Bundle = a directory under `simulation-output/bundles/<runId>/`; `captureBundle` and serializers; commit via `git rev-parse` with graceful fallback; a documented deviation: OPFS isn't copyable so the SQLite "export" is a row-level JSON dump of every app table.)**
- [x] 5.2 Automatic capture on runner failure (wired into 3.1) and on-demand capture command for manual sessions (`sim:repro:capture`). **(`executeScenario` gained an `onFailure` hook fired with the page/context alive before teardown; `sim:repro:capture` CLI added.)**
- [x] 5.3 `simulation/repro/replay.ts` + `sim:repro:replay <bundle> [--build <dir>]`: restore db+storage into a fresh context, re-execute `actions.jsonl`, report step-level divergence. **(`replayBundle` restores rows + storage, replays the action log via the runner's step machinery, emits a step-level divergence report with a `sameFailure` flag.)**
- [x] 5.4 Gitignore + retention: all bundle/artifact paths gitignored; CI retention 7 days; `simulation/repro/README.md` documents the synthetic-data-only rule. **(`/simulation-output/bundles/` gitignored; README documents layout/API/CLI + the synthetic-data-only rule + 7-day retention.)**
- [x] 5.5 Self-test: capture a deliberately failing scenario, replay against the same build (same failure at same step), then against a build with the step's expectation corrected (divergence reported) — proving both spec replay scenarios. **(19 unit tests + a Playwright e2e self-test: same-build replay reproduces the failure at the same step; a corrected expectation (edited `actions.jsonl`) reports divergence — both proven.)**

## 6. Persona and scenario library content

- [x] 6.1 `simulation/personas/`: the six parent personas as typed definitions with behavior parameters sufficient to express their parent journeys. **(Six personas (daily-driver, weekend-returner, power-user, error-prone-user, commuter, new-device-migrator) + `PERSONAS` record; rates mapped from the parent's risk model.)**
- [x] 6.2 `simulation/workflows/`: reusable fragments — `logMeal`, `completePomodoro`, `buildWorkoutRoutine`, `tickHabitGroup`, `onboardFirstTodo`, `changeSetting` — each parameterized and oracle-carrying. **(Six workflows; every mutating step oracle-carrying per the model rule. `completePomodoro` honestly downgraded to the D11 invariant (the clock helper can't fire the app's countdown interval); `changeSetting` is a verification fragment — the catalog has no settings-mutation step yet (documented).)**
- [x] 6.3 Scenarios reproducing parent journeys J1–J10 through the runner (proving the model can express the hand-written suite), coexisting with — not replacing — the parent's files. **(16 scenarios total in `simulation/scenarios/`: J1/J2a/J3/J4/J7 full-arc reproductions; J5/J6/J8/J9/J10 transparent catalog-gap slices (restore step, linked-action authoring step, multi-tab step, settings mutation, HEAVY-seed perf not expressible as steps — each documented in-scenario). All validated; all 16 ran end-to-end deterministic and passed.)**
- [x] 6.4 New composite scenarios the parent does not have: first-run onboarding process; a simulated week of habit tracking (clock-advanced, seeded mistakes); settings-ripple with mid-scenario reload; new-device migration as a composable process. **(Four composites; `week-of-habit-tracking` ran in `seeded` mode with a recorded seed `0x5e1f7e57`, 7 completions / 7 distinct date keys asserted.)**
- [x] 6.5 `simulation/fixtures/`: fixture builders composing the parent's SMALL/TYPICAL/HEAVY seeders, produced only through real data-layer calls (D9). **(apiLeg preamble builders (`todoStep`/`habitStep`/`calorieStep`/`routineStep` + `small/typical/heavyPreamble`); scope limits (no soft-deletes/pomodoro/saved-meals via the apiLeg registry) documented.)**

## 7. AI exploratory lane

- [x] 7.1 `simulation/ai/anomaly-report.ts`: `anomaly-report.json` schema (attempted, expected vs. observed, trace, persisted state, environment, severity guess) + validator; unit-tested. **(Full schema + `validateAnomalyReport`/`parseAnomalyReport`; enforces D7's repro evidence rule; 32 unit tests.)**
- [x] 7.2 `simulation/ai/missions/`: initial missions — at minimum: "Error-Prone User explores Todos+Calories for 20 minutes", "New Device Migrator attempts restore without instructions", "Power User stress-tests linked actions", "Daily Driver uses the app across a simulated midnight" — each with persona, objective, fixture, budget, rubric; no prescribed steps. **(Four missions as structured definitions; each maps known gaps to their companion changes.)**
- [x] 7.3 `simulation/ai/RUNBOOK.md`: how to execute a mission with an external Playwright-MCP-capable agent runtime (this CLI or equivalent), against which build, where reports land, and the triage rule (defect change / new deterministic scenario / documented non-issue). **(Written with exact commands, report layout, validation step before triage.)**
- [ ] **BLOCKED (external)** — 7.4 Execute each initial mission once locally; file or dismiss every anomaly per the triage rule; record outcomes alongside the reports. **(NOT EXECUTED — requires an LLM-capable Playwright-MCP agent runtime that this environment lacks; recorded honestly in `simulation/ai/EXECUTION_STATUS.md` with the maintainer commands; no fake results. The lane stays on-demand/nightly and non-gating.)**
- [x] 7.5 Document the CI wiring open question (runtime + credentials) in the runbook; the lane stays on-demand/nightly and non-gating (D7). **(Covered in RUNBOOK's CI section.)**

## 8. Disposable-backend lane

- [x] 8.1 `simulation/backend/schema.sql`: reference schema for `todos`, `habits`, `calorie_entries`, `workout_routines` + RLS policies, header-commented as a manually maintained copy of the dashboard configuration, written compatible with a future local `supabase start` lane. **(Postgres reference copy byte-compatible with the sync adapter's `SELECT *` + upsert; RLS `FOR ALL TO anon` permissive + grants; idempotent; header marks it as the maintained copy pointing to DRIFT.md.)**
- [x] 8.2 `simulation/backend/provision.ts`: create-or-wipe a throwaway project (Supabase CLI in CI), deploy `parse-ai-command` when the run covers the parser, emit short-lived env for the `dist-live/` build; wipe/teardown at end of run. **(Scripted (create/reuse/check); guard-first before any network; fail-fast preconditions incl. `SIMULATION_PRODUCTION_SUPABASE_HOSTS` so rule (a) is never vacuous; teardown in `finally`. NOT executed here — no Supabase CLI/token in this environment; verified the CLI absence.)**
- [x] 8.3 `simulation/backend/guard.ts` (+ unit tests): abort before any build/network call when the target host matches the production URL, when ambient production credentials are present, or when the project lacks the disposable marker; abort message names the rule fired. **(All three rules implemented + 22 unit tests; wired as the first step of provision.)**
- [x] 8.4 `dist-live/` build script: export pointed at the disposable project, produced only inside the guarded job; never committed. **(`build-dist-live.sh`; `dist-live/` gitignored; execution blocked on 8.2's preconditions.)**
- [ ] **BLOCKED (external)** — 8.5 Backend round-trip scenario set: sync upsert/dedupe/backoff/partial-failure, restore lifecycle (preview → eligibility → import → non-restored entities), edge-function contract — verified from both local SQLite rows and remote table contents. **(Scenario definitions written and validation-clean (`lane: 'disposable'` tags, local oracles + `[remote]` paired assertions prescribed); EXECUTION requires the disposable Supabase project (8.2 preconditions) which this environment lacks — the round-trip runs when the guarded lane runs in CI.)**
- [x] 8.6 Record reference-schema drift procedure: any dashboard-vs-`schema.sql` discrepancy is filed as a finding; note the follow-up change that would move Supabase schema into version control. **(`simulation/backend/DRIFT.md`; follow-up change noted.)**

## 9. CI wiring and environment matrix

- [x] 9.1 `simulation/matrix.ts`: the lane matrix as configuration (backend target, behavior mode, trigger, retention, gates?) consumed by runner and CI; forbidden combinations fail fast (e.g., `seeded` in the PR lane). **(`SIMULATION_LANES` — 8 typed lanes with backend/mode/triggers/gates/retentionDays(7)/scenarioFilter/budgetMs(600_000 for PR); `validateMatrix` flags seeded/exploratory + remote-backed lanes on PR + gating non-deterministic lanes; wired into `sim:validate` + `sim:run`; 6 negative controls verified.)**
- [x] 9.2 `.github/workflows/ci.yml` (or a new `simulation.yml`): PR job running the deterministic scenario subset (≤ 10 min budget); `main` job adding the full library; nightly `schedule` job adding seeded mode + disposable-backend lane. PR lane must not include the disposable backend or the AI lane. **(Extended `ci.yml` to 3 jobs: quality (unchanged gates, `npm test` = unit+integration under `TZ=Asia/Manila`); e2e (PR: `--project=chromium` + `e2e:journeys:p0` + `sim:validate` + `sim:run --mode deterministic --scenario @p0`; main: full `npm run e2e` incl. journeys + simulation self-test + full deterministic `sim:run` + `dist-sync` build; nightly `schedule` adds seeded mode + credential-gated disposable-backend lane); `dist-sync` job (dummy env, main/nightly only). PR never runs the disposable backend or the AI lane.)**
- [x] 9.3 Artifact upload: run reports, failure digests, traces, repro bundles on failure — 7-day retention, matching the existing `e2e-report` convention. **(`simulation-output/` + `e2e-report` uploaded for all lanes at 7-day retention; `dist-sync/` uploaded on main/nightly.)**
- [x] 9.4 Measure and record the PR subset's wall-clock contribution; a miss against the budget is triaged (subset trimmed or budget revisited explicitly), never silently absorbed. **(Local measurement recorded in `simulation/README.md`: `e2e:journeys:p0` ~33–35s + `sim:run @p0` ~37s ≈ 70–75s total, well under the 10-min budget; labelled local, CI wall-clock pending; one observed J1 timer-parse flake (green on re-run, CI retries mask) recorded as a finding.)**

## 10. Gap register and documentation

- [x] 10.1 Extend `docs/testing/known-gaps.md` (created by the parent change) with this platform's capability gaps: native-device exploration, AI-lane non-determinism/cost, manually maintained reference schema, no persistent staging — each with reason and closing path. **(Done — "Platform capability gaps — add-user-simulation-platform" section appended; see the file.)**
- [x] 10.2 Finalize `simulation/README.md`: model authoring guide (persona/scenario/workflow), lane matrix table, repro workflow, isolation rules, and the "which tool for which job" decision rule. **(Done — authoring guide, matrix-backed lane table, repro workflow, isolation rules, baselines/budgets; see the file.)**
- [x] 10.3 Update `AGENTS.md` testing section with the platform layer (one short subsection pointing at `simulation/README.md`) once the runner exists. **(Done — "Simulation platform (user-simulation)" subsection added under Testing Strategy pointing at `simulation/README.md`; CI section updated for the 3 jobs/lanes.)**

## 11. Verification

- [x] 11.1 `npm run typecheck` and `npm run lint` clean (lint warnings under the 81 cap). **(Verified 2026-08-04: typecheck 0 errors; lint 0 errors / 16 warnings, all under the cap.)**
- [x] 11.2 `npm test` — existing unit tests plus new `tests/simulation.*.test.ts` all pass. **(Verified: 630 tests / 56 files = 586 unit (incl. 108 simulation tests across 6 files) + 44 integration.)**
- [x] 11.3 `npm run e2e` — the parent's deterministic suite passes unmodified, CG-1/CG-2 still quarantined under their original companion-change names. **(Verified 2026-08-04: `npm run e2e` = 139 passed / 23 skipped / 0 failed across all three projects; the parent's deterministic suite (chromium + non-quarantined journeys) passes unmodified, and CG-1/CG-2 remain quarantined under their original companion-change names.)**
- [x] 11.4 `npm run sim:validate` clean; `npm run sim:run --mode deterministic` green locally and in CI within the PR budget. **(Verified: `sim:validate` exit 0 (16 scenarios + matrix); the full library ran end-to-end deterministic and passed; local PR-subset wall-clock ~70–75s recorded in `simulation/README.md`, well under the 10-min budget; CI wall-clock pending.)**
- [x] 11.5 Spec conformance sweep: repro replay reproduces and then confirms a fix (5.5); the backend guard's three abort rules each fire in a unit test (8.3); a `seeded`-mode failure is replayed exactly from its recorded seed; every mutating step in the library carries oracles (1.4). **(Repro self-test (5.5) proves same-build reproduction + fixed-expectation divergence; `simulation.backend.guard.test.ts` (22 tests) fires all three abort rules; the seeded `week-of-habit-tracking` ran from a recorded seed `0x5e1f7e57`; `validateSimulationModel` enforces oracle-carrying mutating steps across the whole library.)**
- [x] 11.6 Confirm no application source was modified, no `data-testid` added, no real credentials appear anywhere in the repo or CI config, and every platform finding from 7.4 is resolved as defect / scenario / documented non-issue. **(No app source modified; no `data-testid` added (0 in app + 0 in new specs); CI uses dummy/non-routable env for `dist-sync` and no real credentials anywhere; the CI workflow's dist-sync build is verified to bake the dummy URL only. The "every platform finding from 7.4 resolved" half is N/A — 7.4 (AI mission execution) is blocked on an external agent runtime and has produced no findings yet; recorded in `simulation/ai/EXECUTION_STATUS.md`.)**
