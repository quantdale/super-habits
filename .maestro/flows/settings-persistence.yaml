appId: com.dale16.superhabits
tags:
  - native
  - persistence
  - settings
---
- launchApp:
    clearState: true
# Cold-emulator bootstrap (account-ownership gate) can still be showing its
# 'Preparing local workspace' loader at 10s (Wave 8 screenshot proof).
# 30s matches the launch budgets in calories-persistence/native-smoke.
- extendedWaitUntil:
    visible: 'Today'
    timeout: 30000
- tapOn: 'Open settings'
- extendedWaitUntil:
    visible: 'Close settings'
    timeout: 5000
- tapOn: 'Dark'
- tapOn: 'Close settings'
- killApp
- launchApp
- extendedWaitUntil:
    visible: 'Today'
    timeout: 30000
- tapOn: 'Open settings'
- assertVisible: 'Dark'
- tapOn: 'Close settings'
